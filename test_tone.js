const fs = require('fs');
let html = fs.readFileSync('es/clase-02.html', 'utf8');

// I will insert console.logs to see where it gets stuck, or I'll just change the Tone.Player loading to use the base64ToArrayBuffer we already had!
